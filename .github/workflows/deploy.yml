name: Manual Deploy to GitHub Pages

on:
  workflow_dispatch:  # Manual trigger from GitHub UI

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for Production
        # Stripe publishable key comes from the committed .env.production
        # (client-safe by design; Vite inlines it at build time).
        env:
          # Meta Pixel ID. Set as a repository *variable* (Settings → Secrets and
          # variables → Actions → Variables) — a pixel ID is public by design.
          # Unset = no pixel is injected at all.
          VITE_META_PIXEL_ID: ${{ vars.VITE_META_PIXEL_ID }}
        run: |
          npm run build
          echo "www.rogueautomotiveja.com" > dist/CNAME
          touch dist/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4